const IS_ABSENT = 0
let empHrCheck = Math.floor(Math.random() * 10) % 2;
if (empHrCheck == IS_ABSENT) {
    console.log("UC1- employeeis absent");
}
else {
    console.log("UC1 -employee is present");
}
